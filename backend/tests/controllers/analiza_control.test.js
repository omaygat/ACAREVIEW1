import { analyzeText, analyzeFile } from "../../src/controllers/analiza_control.js";

describe("Controlador analiza_control", () => {

  test("dummy test para que Jest reconozca el archivo", () => {
    expect(true).toBe(true);
  });

  // Puedes agregar tests unitarios reales más adelante:
  // test("analyzeFile devuelve 400 si no hay archivo", async () => { ... });
  // test("analyzeText devuelve resumen correctamente", async () => { ... });
});
